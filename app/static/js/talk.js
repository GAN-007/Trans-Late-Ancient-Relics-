import {$,esc,runTranslation,toast,translationSpeakable} from './core.js';
import {createRecognizer,speak} from './speech.js';

let recognizer=null,listening=false,finalSpeech='';
function render(result){const d=result.deterministic||result,g=d.hieroglyphs||'',preferred=result.ai?.review?.preferred_interpretation?.text||d.literal_gloss||d.translations?.[0]||d.transliteration||d.message||'No secure result.';$('#speechResult').innerHTML=`${g?`<div class="glyph hero-glyph">${esc(g)}</div>`:''}<div class="result-primary">${esc(preferred)}</div>${result.pronunciation?`<div class="pronunciation-box">Classroom reading: ${esc(result.pronunciation.classroom_reading)}</div>`:''}`;}
async function translate(text,live=false){if(!text?.trim())return;$('#speechTranslateBtn').disabled=true;if(live)$('#speechStatus').textContent='Live translating completed utterance…';try{const result=await runTranslation({text:text.trim(),source:$('#speechSource').value,target:$('#speechTarget').value,context:$('#speechContext').value,register:'natural',useAI:true});render(result);if($('#autoSpeak').checked){const item=translationSpeakable(result);if(item)speak(item.text,item.language);}if(live)$('#speechStatus').textContent='Live translation complete. Listening continues.';}catch(e){toast(e.message);}finally{$('#speechTranslateBtn').disabled=false;}}
function configure(){if(recognizer&&listening){try{recognizer.stop();}catch{}}recognizer=createRecognizer({language:$('#speechSource').value,onInterim:t=>$('#speechStatus').textContent=`Hearing: ${t}`,onFinal:t=>{finalSpeech=`${finalSpeech} ${t}`.trim();$('#speechTranscript').value=finalSpeech;$('#speechStatus').textContent='Speech captured. Continue speaking or translate.';if($('#liveSpeechTranslate').checked&&t.trim())translate(t.trim(),true);},onStatus:s=>$('#speechStatus').textContent=s,onError:()=>stopUI(),onEnd:()=>{if(listening)setTimeout(()=>{if(!listening||!recognizer)return;try{recognizer.start();}catch{stopUI();}},250);}});}
function stopUI(){listening=false;$('#listenBtn').classList.remove('listening');$('#listenBtn').setAttribute('aria-pressed','false');$('#listenBtn span').textContent='Start listening';}
export function initTalk(){configure();$('#speechSource').addEventListener('change',configure);$('#listenBtn').addEventListener('click',()=>{if(!recognizer)return toast('Live browser speech recognition is unavailable. Use OS dictation or type the transcript.');if(listening){recognizer.stop();stopUI();return;}finalSpeech=$('#speechTranscript').value.trim();try{recognizer.start();listening=true;$('#listenBtn').classList.add('listening');$('#listenBtn').setAttribute('aria-pressed','true');$('#listenBtn span').textContent='Stop listening';}catch(e){toast(e.message);}});$('#speechTranslateBtn').addEventListener('click',()=>{const text=$('#speechTranscript').value.trim();if(!text)return toast('Say or type something first.');translate(text,false);});}
