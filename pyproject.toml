[project]
name = "trans-late-ancient-relics"
version = "3.0.0"
description = "Evidence-aware Ancient Egyptian epigraphy, contextual translation, live vision/voice interaction and reversible ESHB tools."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115,<1.0",
  "uvicorn[standard]>=0.30,<1.0",
  "pydantic>=2.7,<3.0",
  "httpx>=0.27,<1.0"
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
